[project]
name = "reporter"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "rash-database",
    "rash-observability",
    "boto3>=1.40.9",
    "langfuse>=3.3.4",
    "openai-agents[litellm]>=0.2.6",
    "pydantic>=2.11.7",
    "pydantic-ai>=1.0.6",
    "python-dotenv>=1.1.1",
    "tenacity>=9.1.2",
]

[tool.uv.sources]
rash-database = { path = "../database", editable = true }
rash-observability = { path = "../observability", editable = true }
